# Optional. The booth host answers from the printed board without this.
# With a key, replies get a bit more conversational via SpaceXAI (xAI).
# Never commit a real key. Server-side only.
XAI_API_KEY=
